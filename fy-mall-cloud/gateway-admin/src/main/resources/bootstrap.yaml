spring:
  application:
    name: gateway-admin
  cloud:
    nacos:
      config:
        server-addr: 101.43.163.223:8848
        file-extension: yaml
        group: FY_MALL_GROUP
        # 通用配置文件
        shared-configs:
          - data-id: common-nacos.yaml
            group: FY_MALL_GROUP
            refresh: true
  # classpath 下包含web-mvc 需添加该配置
  main:
    web-application-type: reactive
  # redis
  redis:
    host: 101.43.163.223
    password: Turing