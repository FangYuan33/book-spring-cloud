spring:
  application:
    name: user-service
  cloud:
    nacos:
      config:
        server-addr: 101.43.163.223:8848
        file-extension: yaml
        group: FY_MALL_GROUP
        # 通用配置文件
        shared-configs:
          - data-id: common-nacos.yaml
            group: FY_MALL_GROUP
            refresh: true
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    name: fy-mall-cloud-user-service
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://101.43.163.223:3306/fy_mall_user?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&useSSL=false&allowMultiQueries=true
    username: root
    password: Turing
  # 解决Swagger空指针异常
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  # redis
  redis:
    host: 101.43.163.223
    password: Turing