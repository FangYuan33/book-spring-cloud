# Tomcat
server:
  port: 9201

# Spring
spring: 
  application:
    # 应用名称
    name: ruoyi-system
  profiles:
    # 环境配置
    active: dev
  datasource:
    dynamic:
      primary: master
      datasource:
        master:
          url: jdbc:mysql://101.43.163.223:3306/ry-cloud?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&useSSL=false&allowMultiQueries=true
          username: root
          password: Turing
          driver-class-name: com.mysql.cj.jdbc.Driver
          type: com.alibaba.druid.pool.DruidDataSource
          druid:
            initial-size: 5
            max-active: 20
            min-idle: 5
            max-wait: 60000
  redis:
    host: 101.43.163.223
    password: Turing
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 101.43.163.223:8848
      config:
        # 配置中心地址
        server-addr: 101.43.163.223:8848
        # 配置文件格式
        file-extension: yaml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
          - data-id: common-nacos.yaml
            group: FY_MALL_GROUP
            refresh: true
mybatis:
  mapper-locations: classpath:mapper/system/*Mapper.xml
  type-aliases-package: com.ruoyi.system.domain,com.ruoyi.system.api.domain